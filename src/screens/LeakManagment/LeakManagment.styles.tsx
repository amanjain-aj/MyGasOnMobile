import { StyleSheet } from 'react-native'
import config from '../../config/colors'

export default StyleSheet.create({
    contianer: {
        width: '100%',
        height:'100%'
    },
    rowView: {
        flexDirection: 'row',
        height:70,
        marginBottom:0,
        alignItems:'center'
    }
})